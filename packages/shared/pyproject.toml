[project]
name = "shared"
version = "0.1.0"
description = "Common types, config, and utilities shared across agentic-rag-platform packages."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared"]
